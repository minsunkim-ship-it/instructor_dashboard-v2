# Service Checklist

## Role
이 문서는 다음 프로젝트를 시작할 때 바로 쓰는 개인 템플릿이다.
목적은 이번 프로젝트에서 놓쳤던 큰 범주의 문제를 다음 프로젝트에서 초기에 차단하는 것이다.

## 사용 원칙
- 각 항목은 `완료`, `의도적 제외`, `불필요` 중 하나로 판정한다.
- `의도적 제외`면 이유와 영향을 남긴다.
- `미정` 상태 항목이 남아 있으면 완료 선언을 하지 않는다.

## 1. 문서-구현 불일치

### 시작 전
- [ ] 이번 프로젝트의 필수 사용자 흐름을 3~5개로 적었다.
- [ ] 인증, 권한, 핵심 API, 핵심 화면 책임을 문서로 먼저 정했다.
- [ ] 화면 노출 기준과 내부 집계 기준을 문서로 먼저 정했다.
- [ ] “이번에 안 넣을 것”과 “넣어야 하는데 아직 없는 것”을 구분해서 gap으로 적었다.
- [ ] 같은 provider를 쓰는 source 묶음의 인증 전략을 한 줄로 적었다.
  - 예: 모든 Google Sheets source는 user OAuth refresh token 사용

### 구현 중 / 종료 전
- [ ] 문서에 있는 P0 규칙이 실제 코드에 반영됐는지 대조했다.
- [ ] 문서에 없는 규칙을 구현 중 즉석 결정하지 않았는지 확인했다.
- [ ] 목록, 상세, 점수/집계가 같은 기준을 쓰는지 확인했다.
- [ ] 빠진 문서 항목이 있으면 완료가 아니라 gap으로 기록했다.
- [ ] `.env.example`, 문서, 런타임 코드가 같은 env surface를 쓰는지 확인했다.

## 2. 배포 / Env

### 배포 전
- [ ] 필수 env 목록을 정리했다.
- [ ] 로컬 전용 값과 배포 전용 값을 구분했다.
- [ ] 외부 연동별로 필요한 자격증명, 권한, 파일/경로 의존성을 적었다.
- [ ] 더 이상 안 쓰는 env를 템플릿/문서에서 제거했다.

### 배포 직후
- [ ] 메인 URL이 열린다.
- [ ] 상태/헬스 체크 API가 기대값으로 응답한다.
- [ ] 실제 데이터가 필요한 핵심 API가 응답한다.
- [ ] env 저장 후 재배포/재시작이 실제 반영됐는지 확인했다.
- [ ] “배포가 떴다”와 “서비스가 정상 동작한다”를 같은 뜻으로 쓰지 않았다.

## 3. 외부 Source Runtime

### 설계 전 / 구현 전
- [ ] 외부 source별 timeout이 있다.
- [ ] 외부 source별 budget이 있다.
- [ ] 외부 source별 concurrency limit이 있다.
- [ ] 한 source 실패가 전체 작업을 영구 block하지 않게 설계했다.
- [ ] `success`, `partial`, `failed` 기준을 먼저 정의했다.
- [ ] 장시간 route에 stage-level progress를 남길 계획이 있다.

### 검증 전
- [ ] route 연결 전에 source 단독 실행을 먼저 확인했다.
- [ ] 종료 보장 없이 운영 route에 연결하지 않았다.
- [ ] 외부 source 권한 문제를 실제 계정/실제 자격증명으로 확인했다.
- [ ] hang 또는 `running` 잔재를 정리하는 방법이 있다.
- [ ] collector 단계와 store 단계의 병목을 구분할 수 있는 로그/진행 surface가 있다.

## 4. 데이터 품질

### 구현 전
- [ ] 정상 케이스를 적었다.
- [ ] 경계 케이스를 적었다.
- [ ] 오분류/오해석 가능성이 높은 반례를 적었다.
- [ ] raw 보존과 파생값 계산, 화면 노출 규칙을 분리했다.
- [ ] fallback 우선순위를 먼저 정했다.
- [ ] source별 소유 필드를 먼저 정했다.
- [ ] 원본 식별자 dedupe와 business row dedupe를 구분했다.

### 검증 전 / 종료 전
- [ ] “응답이 나온다”와 “데이터가 맞다”를 분리해서 확인했다.
- [ ] source별로 `수집 성공`, `매칭/반영 성공`, `화면 품질 통과`를 따로 봤다.
- [ ] 빈 값, raw 문자열 노출, 잘못된 grouping/집계가 없는지 확인했다.
- [ ] 대표 반례를 실제 데이터로 다시 확인했다.
- [ ] list/detail/API/집계가 같은 dedupe 기준을 쓰는지 확인했다.
- [ ] rerun 시 no-op update amplification이 없는지 확인했다.

## 5. 완료 판정 기준

### 완료 선언 전
- [ ] 구현 성공
- [ ] 빌드 성공
- [ ] 배포 성공
- [ ] runtime 성공
- [ ] 데이터 품질 통과
- [ ] clean rerun에서 수렴한다 (`updated=0` 또는 동등한 기준)

### 보고 전
- [ ] 현재 어디까지 성공했는지 단계별로 적었다.
- [ ] 남은 문제를 구현 문제, 배포/env 문제, 외부 source 문제, 데이터 품질 문제로 나눠 적었다.
- [ ] “기술적으로 동작”과 “운영 준비 완료”를 같은 뜻으로 쓰지 않았다.
- [ ] 위 다섯 단계를 통과하기 전에는 완료/MVP 완료라고 말하지 않았다.
