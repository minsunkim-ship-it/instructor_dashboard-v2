# Service Checklist

## Role
이 문서는 혼자 규모가 작지 않은 서비스를 만들 때, 같은 실수를 반복하지 않기 위한 개인용 시작/마무리 체크리스트다.

## 사용 원칙
- 이 체크리스트는 아래 5가지를 놓치지 않기 위한 용도다.
  - 문서-구현 불일치
  - 배포/env 누락
  - 외부 source hang
  - 데이터 품질 오판정
  - 완료 판정 기준 흐림
- 단순 구현 디테일, 일회성 버그, 다시 안 쓸 세부 상황은 적지 않는다.

## A. 문서-구현 일치

### 시작 전
- [ ] 필수 사용자 흐름을 문서 기준으로 3~5개 적었다.
- [ ] 인증/권한이 문서에 있으면 실제 구현 계획에도 들어가 있다.
- [ ] 핵심 API 엔드포인트와 화면 책임 분리가 문서에 정리돼 있다.
- [ ] “나중에 붙일 예정”인 필수 규칙은 gap으로 따로 적었다.

### 완료 전
- [ ] 인증/권한이 실제로 동작한다.
- [ ] 문서에 있는 필수 엔드포인트가 실제로 응답한다.
- [ ] 문서에 있는 핵심 화면 흐름이 실제로 가능하다.
- [ ] 책임 분리 규칙(누가 어떤 파일/영역 담당인지)이 코드에도 반영돼 있다.

## B. 배포 / 환경변수

### 배포 전
- [ ] 필수 env 목록이 정리돼 있다.
- [ ] 서비스 런타임이 실제로 읽어야 하는 env와 외부 연동 env를 구분했다.
- [ ] 로컬 절대경로가 배포 환경에 그대로 들어가는 값이 없는지 확인했다.

### 배포 직후
- [ ] 메인 URL이 응답한다.
- [ ] 헬스 체크 API가 200 또는 기대한 상태로 응답한다.
- [ ] DB 연결이 필요한 실제 데이터 API가 응답한다.
- [ ] 필수 env 누락 시 어떤 증상이 나는지 확인했다.
- [ ] 저장 후 재배포/재시작이 실제로 반영됐는지 확인했다.

## C. 외부 source / runtime

### 설계 시
- [ ] 각 source collector에 request timeout이 있다.
- [ ] 각 source 또는 scope에 budget이 있다.
- [ ] 병렬 처리 개수(concurrency limit)가 있다.
- [ ] source 실패가 전체 서비스를 영구 block하지 않도록 설계돼 있다.

### 검증 시
- [ ] source 단독 benchmark를 먼저 돌렸다.
- [ ] route 전체 실행 전에 source 단위 종료 보장을 확인했다.
- [ ] `running` 상태 잔재를 정리하는 방법이 있다.
- [ ] collector 성공과 applier 성공을 구분해서 봤다.
- [ ] source `success`가 “예외 없음”만 의미하지 않도록 `partial` 기준을 정의했다.
- [ ] source별 raw 수집 건수와 canonical 반영 건수를 운영자가 바로 볼 수 있다.

## D. 데이터 품질

### 구현 전
- [ ] 정상 케이스 1~2개를 적었다.
- [ ] 경계 케이스 1~2개를 적었다.
- [ ] 오분류 가능성이 높은 반례 1개 이상을 적었다.
- [ ] 정책 문구가 애매한 곳은 코드로 먼저 해석하지 않고 문서 기준을 정했다.

### 검증 시
- [ ] 응답이 나온다는 것과 데이터가 맞다는 것을 분리해서 봤다.
- [ ] sample expected/actual 비교를 했다.
- [ ] 실데이터 sanity check를 했다.
- [ ] “현재 성공은 파이프라인 성공인지, 데이터 품질 통과인지”를 구분해서 적었다.
- [ ] source별로 “수집 성공”, “매칭/반영 성공”, “실제 점수 반영 성공”을 따로 본다.

## E. 완료 판정

### 구분해서 판단할 것
- [ ] 구현 성공
- [ ] 빌드/배포 성공
- [ ] route/runtime 성공
- [ ] 데이터 품질 통과

### 최종 보고 전
- [ ] 지금 어디까지 성공했는지 단계별로 적었다.
- [ ] 남은 blocker가 구현 문제인지, 외부 runtime 문제인지 분리했다.
- [ ] MVP 완료라고 말해도 되는지 다시 확인했다.
- [ ] “기술적으로 동작”과 “운영 준비 완료”를 같은 말로 쓰지 않았다.

## F. 최소 헬스 체크 세트

- [ ] 메인 페이지 로딩
- [ ] 상태 API 응답
- [ ] 핵심 목록 API 응답
- [ ] 핵심 저장/새로고침 API 응답
- [ ] 핵심 사용자 흐름 1회 수동 확인
